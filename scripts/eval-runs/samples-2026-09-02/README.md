# Generated samples, 2026-09-02

Live generations against the fixture leagues (`src/lib/ai/__fixtures__/rich-week.json`, `draft-day.json`)
after the routing, prose-register, and thin-article fixes. Every name here is invented fixture data.
The HTML comment at the end of each file is the verifier's flag list for that article.

- `weekly_recap--curtis-vaughn` — the anchor's recap (Opus 5)
- `weekly_recap--mel-diaper` — Mel's recap with receipts and one admitted miss (Opus 5)
- `weekly_preview--curtis-vaughn` — a true look-ahead on the week-8 slate (Sonnet 5)
- `draft_rankings--mel-diaper` — draft grades, pick/ADP/delta on every line (Opus 5)
- `trade_analysis--dex-alvarez` — Reported / Opinion tiers, plain-English timestamp (Opus 5)

Regenerate with `npm run eval:articles -- --live --type <type> --persona <slug> --fixture <fixture> --dump <dir>`.
